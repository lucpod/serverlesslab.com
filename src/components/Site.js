import { h } from 'preact'

const Site = () => (
  <html>
    <div>
      <h5>Currently work in progress</h5>
      <p>a bit of patience is in order...</p>
    </div>
  </html>
)

export default Site
